<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dog Picture</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            text-align: center;
        }
        img {
            max-width: 400px;
            height: auto;
            border-radius: 8px;
            margin-bottom: 20px;
        }
        .button {
            margin: 20px;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            background-color: #007bff;
            color: white;
            text-decoration: none;
        }
        .button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <h1>Dog Picture</h1>
    <img src="<%= dogPic.imageUrl %>" alt="Dog Picture">
    <p><strong>Description:</strong> <%= dogPic.description || 'No description provided' %></p>
    <p><strong>Uploaded by:</strong> <%= dogPic.user.username || 'Unknown' %></p>

    <a href="/list" class="button">Back to Gallery</a>
    <a href="/edit/<%= dogPic.id %>" class="button">Edit Picture</a>
    <form action="/delete/<%= dogPic.id %>" method="POST" style="display: inline;">
        <button type="submit" class="button" style="background-color: #f44336;">Delete Picture</button>
    </form>
</body>
</html>
